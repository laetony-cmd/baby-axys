# Configuration Nixpacks pour Railway
# Force rebuild complet - 26 Dec 2025

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install --no-cache-dir -r requirements.txt"]

[start]
cmd = "python main.py"
